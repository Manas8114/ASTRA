services:
  # ── Always-on demo services ───────────────────────────────────────────────

  twin-service:
    build:
      context: .
      dockerfile: twin-service/Dockerfile
    container_name: astra-twin
    ports:
      - "50051:50051"
    restart: unless-stopped
    networks:
      - astra-net

  astra:
    build:
      context: .
      dockerfile: xapp/Dockerfile
    container_name: astra-xapp
    env_file: .env
    environment:
      - TWIN_SERVICE_HOST=twin-service
      - TWIN_SERVICE_PORT=50051
    ports:
      - "8000:8000"
    volumes:
      - model-artifacts:/app/xapp/model/saved_models
      - training-data:/app/training/data
      - ./data:/app/data
    depends_on:
      - twin-service
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 15s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - astra-net

  dashboard:
    image: node:20-alpine
    container_name: astra-dashboard
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - "3000:3000"
    volumes:
      - ./dashboard:/app
      - dashboard-node-modules:/app/node_modules
    depends_on:
      - astra
    restart: unless-stopped
    networks:
      - astra-net

  # ── CR²E — Causal RAN Root-Cause Engine (demo mode, always-on) ──────────────

  cr2e:
    build:
      context: .
      dockerfile: cr2e/Dockerfile
    container_name: cr2e-engine
    env_file: .env
    environment:
      - CR2E_MODE=demo
      - ASTRA_BASE_URL=http://astra:8000
      - CR2E_OLLAMA_URL=http://host.docker.internal:11434
      - CR2E_MLFLOW_URI=http://mlflow:5000
    ports:
      - "8001:8001"
    volumes:
      - cr2e-dag-store:/app/cr2e/data
      - ./cr2e:/app/cr2e
    depends_on:
      - astra
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/cr2e/health')"]
      interval: 20s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - astra-net

  mlflow:
    image: ghcr.io/mlflow/mlflow:v2.14.0
    container_name: cr2e-mlflow
    command: mlflow server --host 0.0.0.0 --port 5000
    ports:
      - "5000:5000"
    volumes:
      - mlflow-artifacts:/mlruns
    restart: unless-stopped
    networks:
      - astra-net

  # ── Lab profile services (opt-in with --profile lab) ─────────────────────

  mongodb:
    image: mongo:7
    container_name: astra-mongodb
    profiles: ["lab"]
    volumes:
      - mongodb-data:/data/db
    restart: unless-stopped
    networks:
      - astra-net

  open5gs:
    image: gradiant/open5gs:2.7.2
    container_name: astra-open5gs
    profiles: ["lab"]
    depends_on:
      - mongodb
    ports:
      - "9999:9999/udp"
    restart: unless-stopped
    networks:
      - astra-net

  kpi-exporter:
    build:
      context: .
      dockerfile: core/Dockerfile
    container_name: astra-kpi-exporter
    profiles: ["lab"]
    env_file: .env
    ports:
      - "9090:9090"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - astra-net

  flexric:
    image: ubuntu:22.04
    container_name: astra-flexric
    profiles: ["lab"]
    command: ["sh", "-c", "echo 'FlexRIC placeholder — mount/build your lab RIC here' && sleep infinity"]
    ports:
      - "36421:36421/sctp"
    restart: unless-stopped
    networks:
      - astra-net

  prometheus:
    image: prom/prometheus:latest
    container_name: astra-prometheus
    profiles: ["lab"]
    ports:
      - "9091:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    restart: unless-stopped
    networks:
      - astra-net

  grafana:
    image: grafana/grafana:latest
    container_name: astra-grafana
    profiles: ["lab"]
    ports:
      - "3001:3000"
    volumes:
      - grafana-data:/var/lib/grafana
    restart: unless-stopped
    networks:
      - astra-net

networks:
  astra-net:
    name: astra-net

volumes:
  model-artifacts:
  training-data:
  dashboard-node-modules:
  mongodb-data:
  grafana-data:
  cr2e-dag-store:    # persists discovered DAG snapshots across restarts
  mlflow-artifacts:  # MLflow experiment tracking artifacts
